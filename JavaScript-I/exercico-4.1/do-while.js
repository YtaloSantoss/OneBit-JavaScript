let velocidade  = 20

do {
    alert("velocidade do veículo " + velocidade + "km/h")
    velocidade -= 20
} while (velocidade > 0)

alert("Velocidade final: " + velocidade + " km/h") 
alert("Velocidade parou") 